- Privacy-first? The LLM generates the code to clean the data, and the data stays local and cleaned by run python codes
  - But: LLM can get insights from the data, clean messy and typos in the language, etc.. better then tools
  - Leave the choice to the user

- Generative AI is powerful but imperfect, especially with large or messy data, so don’t expect one prompt to do everything. Work in sections and iterate: explore first, identify issues next, then refine—just like real analysts do.